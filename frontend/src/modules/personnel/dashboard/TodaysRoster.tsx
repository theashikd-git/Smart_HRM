// Superseded by ./TodaysAttendance.tsx (renamed for accuracy — this shows real
// attendance punches, not planned roster assignments, since Roster doesn't
// exist yet). Kept as an empty stub because this file can't be deleted from
// here; nothing imports it.
export {};
